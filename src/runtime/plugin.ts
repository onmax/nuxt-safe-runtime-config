import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin((_nuxtApp) => {
  // This plugin exists for future extensibility but currently does nothing
  // All validation happens at build time to avoid runtime performance impact
})
